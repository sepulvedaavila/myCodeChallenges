#include <stdio.h>
int main(int argc, const char * argv[]) {
    FILE *file = fopen("/home/carlos/Documents/CodeEval/TestFiles/NameToDigit", "r");
    char line[1024];
    while (fgets(line, 1024, file)) {
        
    }
    return 0;
}
